import React from "react";

const Header = () => {
  return (
    <header className="todo-head">
      <span>My Todo List</span>
      <span>React</span>
    </header>
  );
};

export default Header;
